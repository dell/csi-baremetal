global:
  registrySecret:

image:
  registry: docker.io/objectscale
  tag: 0.0.13-376.008bda2
  pullPolicy: Always

env:
  test: false

# logging settings
log:
  format: text
  level: info

# to work only with node with such label
nodeSelector:
  key:
  value:

csi:
  deploy: false
  drivemgr: basemgr

feature:
  useexternalannotation: false
  nodeIDAnnotation:
